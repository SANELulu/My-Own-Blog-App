const User = require('./user');

// User.hasMany(blogPost, {
//     foreignKey: 'user_id',
//     onDelete: 'Cascade',
// });

// blogPost.belongsTo(User, {
    
// });

// module.exports = { User, blogPost};